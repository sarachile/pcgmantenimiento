rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {

    function isSignedIn() {
      return request.auth != null;
    }

    // Ruta para evidencias y firmas de OTs
    // Se permite lectura pública para facilitar la visualización en reportes y emails
    match /companies/{companyId}/workOrders/{workOrderId}/{fileName} {
      allow read: if true;
      allow write: if isSignedIn();
    }
    
    // Ruta para logotipos de empresa
    match /companies/{companyId}/branding/{fileName} {
      allow read: if true;
      allow write: if isSignedIn();
    }

    match /{allPaths=**} {
      allow read, write: if isSignedIn();
    }
  }
}
