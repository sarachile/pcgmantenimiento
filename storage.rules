rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // REGLAS DE EMERGENCIA: Permite lectura y escritura total para evitar bloqueos en terreno.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}